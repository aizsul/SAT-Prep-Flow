## Packages
framer-motion | Smooth transitions between questions and modules
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- "Bluebook" aesthetic requires specific fonts: Source Serif 4 (passages) and Inter (UI).
- Desmos calculator will be embedded via iframe.
- Test flow state will be managed via React Context to persist across question navigation.
